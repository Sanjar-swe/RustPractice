// fn main() {
//     println!("Hello, world!");
// }

//
// cargo new hello_cargo - создание проекта со стандартной директорией
// cd hello_cargo
// cargo run main.rs скомпилирует проект и запустит

// cargo build - создание исполняемого файла

// cargo check - Эта команда быстро проверит ваш код на ошибки компиляции, но не будет создавать исполняемый файл. Это быстрее, чем cargo build, и полезно для частых проверок во время разработки.

// cargo test - запускает тесты

//
//
//

// Отлично! Переходим к Задаче 3: Базовый синтаксис Rust. Сейчас мы рассмотрим основные строительные блоки языка: переменные, типы данных, функции и комментарии.
// Вы можете модифицировать файл src/main.rs в вашем проекте hello_cargo, чтобы попрактиковаться.

//     Переменные:
//         Объявляются с помощью let. По умолчанию они неизменяемы (immutable).
//         Чтобы сделать переменную изменяемой, используйте mut: let mut age = 30; age = 31;
//         Можно "перезатенять" (shadowing) переменные: let x = 5; let x = x + 1;
//     Типы данных:
//         Скалярные:
//             Целые числа: i8, u8, i32 (по умолчанию), u64, isize (размер указателя) и т.д.
//             Числа с плавающей точкой: f32, f64 (по умолчанию).
//             Булевы: true, false.
//             Символы: char (один символ Unicode, в одинарных кавычках: 'a', '😊').

// Составные:

//     Кортежи (Tuples): Фиксированный размер, могут содержать разные типы. let tup: (i32, f64, u8) = (500, 6.4, 1); Доступ по индексу: tup.0.
//     Массивы (Arrays): Фиксированный размер, все элементы одного типа. let a: [i32; 5] = [1, 2, 3, 4, 5]; Доступ по индексу: a[0].

// Rust часто может вывести тип сам (let x = 5; // x будет i32), но иногда нужно указывать явно (let guess: u32 = "42".parse().expect("Not a number!");).

// Функции:

//     Объявляются с помощью fn.
//     Имена функций и переменных используют snake_case (слова_в_нижнем_регистре_через_подчеркивание).
//     Можно указывать типы параметров и тип возвращаемого значения.
//     Последнее выражение в функции без точки с запятой является возвращаемым значением.

// fn main() {
//     println!("Hello world");
//     another_function(5, 'h');
//     let sum = add_five(5);
//     println!("Sum is: {}", sum);
// }

// fn another_function(x: i32, unit_label: char) {
//     println!("The value of x is: {}{}", x, unit_label);
// }

// fn add_five(x: i32) -> i32 {
//     x + 5
// }

// Комментарии:

//     Однострочные: // Это комментарий
//     Многострочные: /* Это многострочный комментарий */
// //     Документационные комментарии (для cargo doc): /// Комментирует следующий элемент или //! Комментирует содержащий элемент (например, модуль или крейт).
// fn main() {
//     // Неизменяемая переменная
//     let lucky_number = 7;
//     // Изменяемая переменная
//     let mut count = 0;

//     println!("Initial count: {}", count);
//     count = count + 1;

//     // вызов функции
//     print_info(lucky_number, count);
// }

// /// Эта функция печатает информацию
// fn print_info(num: i32, current_count: i32) {
//     println!("Lucky_number is: {}", num);
//     println!("current_count is: {}", current_count);
//     let is_lucky = true;
//     let initial = 'L';
//     println!("Is it lucky? {}, Initial: {}", is_lucky, initial);
// }
